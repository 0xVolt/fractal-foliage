// Variables -> (a, b)
// Axiom -> A
// Ruleset: {
//    (A -> AB),
//    (B -> A)
// }

// Start out with our axiom
var axiom = "A";

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
}
